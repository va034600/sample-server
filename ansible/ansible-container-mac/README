参考

install ansible-container
https://docs.ansible.com/ansible-container/installation.html#running-from-source

AnsibleでDockerコンテナを作成する「Ansible Container」を使ってみる
http://knowledge.sakura.ad.jp/knowledge/9473/

新しいAnsible Containerを使うとPlaybookだけでDockerイメージをビルドできる
http://qiita.com/kjtanaka/items/ebf38ead92e4aa1fb50a


ansible-container 0.9.2 インストール(for mac)

$ git clone https://github.com/ansible/ansible-container.git
$ cd ansible-container
$ pip install -e .[docker]
$ pip install --upgrade setuptools




$ pip install ansible-container[docker]


$ ansible-container build
$ ansible-container run

